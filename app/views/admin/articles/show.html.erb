<table class="table">
  <thead>
    <tr>
      <th>Section</th>
      <th>Actions</th>
    </tr>
  </thead>

	<% @article.sections.each do |section| %>
		<tr>
			<td><%= section.content %></td>
      <td><%= link_to "Edit", [:edit, :admin, @article, section], class: "btn btn-default" %></td>
		</tr>
	<% end %>
</table>