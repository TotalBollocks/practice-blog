<table>
	<tr>
		<th>Article</th>
		<th>Action</th>
	</tr>
	<% @subject.articles.each do |article| %>
		<tr>
			<td><%= article.title %></td>
			<td><%= link_to "Edit Info", [:edit, :admin, article] %></td>
      <td><%= link_to "Edit sections", [:admin, article] %></td>
		</tr>
	<% end %>
</table>

<%= link_to "Add new article", [:new, :admin, :article] %>