<table>
	<tr>
		<th>Subject Name</th>
		<th>Actions</th>
	</tr>
	<% @subjects.each do |subject| %>
		<tr>
			<td><%= subject.name %></td>
			<td>
        <%= link_to "Rename", [:edit, :admin, subject] %>
        <%= link_to "Manage Articles", [:admin, subject] %>
        <%= link_to "Delete", [:admin, subject], confirm: "Are you sure?", method: :delete %>
			</td>
		</tr>
	<% end %>
</table>

<%= link_to "New subject", new_admin_subject_path %>