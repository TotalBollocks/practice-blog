<div class="row">
  <div class="col-sm-6 col-sm-offset-3">
    <%= render partial: 'shared/errors', locals: {object: @article} %>
  </div>
</div>

<div class="row">
  <div class="col-sm-6 col-sm-offset-3">
    <%= form_for @article do |f| %>
      <%= render partial: 'form', locals: {f: f} %>
    <% end %>
  </div>
</div>

<%= link_to "Add new section", [:new, :admin, @article, :section] %>